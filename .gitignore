node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Local AI skills (re-installable via skills-lock.json + `npx skills add`)
.agents/

# Generated Imagen prompts (re-created by `npm run assets:prompts` from src/video-plan.json)
assets/prompts/

# Generated assets — images + manifest.json (re-created by `npm run assets:gen` from cached prompts)
public/assets/

# Generated video plan is committed so a fresh clone can `npm run dev` without
# first running `npm run ingest`. Re-running ingest will overwrite it.
# (kept un-ignored on purpose — was previously listed here)

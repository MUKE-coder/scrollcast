/**
 * Phase 4 — emit per-asset Gemini Imagen prompts (theme-aware) to `assets/prompts/*.json`.
 * Stubbed in Phase 0 so `npm run assets:prompts` exists as a wired command.
 */
import "dotenv/config";

async function main() {
  console.log("[assets:prompts] not implemented yet — lands in Phase 4.");
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});

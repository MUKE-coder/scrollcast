/**
 * Phase 3 — parse `topic-details.md` (root) → `src/video-plan.json`.
 * Stubbed in Phase 0 so `npm run ingest` exists as a wired command.
 */
import "dotenv/config";

async function main() {
  console.log("[ingest] not implemented yet — lands in Phase 3.");
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
